extends layouts/main

block content
    .form-container    
        .record-container#jsRecordContainer
            <div class="wrapTitle">
            <div class=topLogintitle>동영상 업로드</div> 
            br
            video#jsVideoPreview
            button#jsRecordBtn 녹화 시작하기
        form(action=`/videos${routes.upload}`, method="post", enctype="multipart/form-data")
            div.fileUpload
                label(for="file") 또는, 동영상 파일을 선택하여 업로드<br><br>
                input(type="file", id="file", name="videoFile", required=true, accept="video/*")
            input(type="text", placeholder="제목", name="title", required=true)
            textarea(name="description", placeholder="설명", required=true)
            input(type="submit", value="비디오 업로드")